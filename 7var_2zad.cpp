#include <iostream>
#include <string>
#include <cmath>


void calculation()
{
	float x = 0, c = 0, m=0, z=0;
	std::cin >> x;
	std::cin >> c;
	z = (tan((M_PI*m)/(m+pow(10,-3))))*(tan((M_PI*m)/(m+pow(10,-3))));
	m = exp(-c*abs(x)+sqrt(c*x));
	std::cout << "z" << z <<"\n";
	std::cout << "m" << m;
}
int main()
{

	calculation();
}
